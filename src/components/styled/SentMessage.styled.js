import styled from "styled-components";

//message
export const SentMessage = styled.div`
    height: auto;
    width: 75%;
    background: var(--blue);
    border-radius: 5px;
    margin: 5px 0;   
    align-self: flex-end; 
    text-align: right;
`