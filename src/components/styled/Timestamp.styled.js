import styled from "styled-components";

//used in: 
//  message
//  received message

export const Timestamp = styled.p`
    font-size: 0.6rem;
    color: var(--green);
    padding: 0 10px;
    margin-top: 5px;
`